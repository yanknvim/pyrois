module Top (
    clk: input clock,
    rst: input reset,
) {
    inst ibus: InstMembus;
    inst dbus: DataMembus;

    inst core: Core (
        clk,
        rst,
        ibus: ibus,
        dbus: dbus,
    );

    inst mem: Memory (
        ibus: ibus,
        dbus: dbus,
    );
}
